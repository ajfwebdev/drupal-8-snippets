services:
  my_module.rating_service:
    class: '\Drupal\my_module\MyService'
    arguments: ['@config.factory', '@http_client', '@current_user', '@serialization.json', '@logger.factory', '@request_stack']
